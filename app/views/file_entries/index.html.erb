<h1>Lista Plików</h1>
<ul>
  <% @file_entries.each do |file_entry| %>
    <li>
    <%= link_to file_entry.file.filename, file_entry %>
    <%= button_to 'Delete', file_delete_path(file_entry), method: :delete, data: { confirm: 'Are you sure?' } %>
    <%= link_to 'rename', file_edit_path(file_entry), method: :edit%>
    <%= link_to 'move', move_file_entry_path(file_entry), method: :edit%>
    </li>
  <% end %>
</ul>
<%= link_to 'Nowy Plik', new_file_entry_path %>
